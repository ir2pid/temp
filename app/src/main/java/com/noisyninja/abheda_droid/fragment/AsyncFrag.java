package com.noisyninja.abheda_droid.fragment;

import android.os.AsyncTask;
import android.support.v4.app.Fragment;

/**
 * Created by ir2pi on 12/6/2014.
 */
public class AsyncFrag extends Fragment{
/*
    public Task task;

    public class Task extends AsyncTask<IAsyncFrag, Integer, String> {

        IAsyncFrag iAsyncFrag;

        @Override
        protected String doInBackground(IAsyncFrag... params) {
            iAsyncFrag = params[0];
            iAsyncFrag.run("");
            return null;
        }

        @Override
        protected void onPostExecute(String result) {
            iAsyncFrag.finish(result);
        }

        @Override
        protected void onPreExecute() {
            iAsyncFrag.start();
        }

        @Override
        protected void onProgressUpdate(Integer... values) {
            iAsyncFrag.update(values);
        }
    }*/
}
